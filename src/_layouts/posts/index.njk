---
layout: base.njk
eleventyComputed:
  eleventyNavigation:
    key: "{{ page.lang }}Posts"
    parent: "{{ page.lang }}Frontpage"
---
<h1>{{ title }}</h1>
{{ content | safe }}
{% for post in collections.posts[page.lang] | reverse %}
  <article>
    <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
    <p>
      <time datetime="{{ post.date | dateToRfc3339 }}">
        {{- post.date | date(page.lang) -}}
      </time>
    </p>
  </article>
{% endfor %}
